meta {
  name: 02_[HP] POST /favorites
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/favorites
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer token={{airportgap_token}}
}

body:json {
  {
    "airport_id": "VIE"
  }
}

body:form-urlencoded {
  from: VIE
  to: LHR
}

tests {
  bru.setNextRequest("03_[HP] GET /favorites");
}

settings {
  encodeUrl: true
  timeout: 0
}
